=== Core Functionality ===

This contains all your site's core functionality so that it is theme independent.

= MU-Plugins =
1. Place `Core-Functionality.php` and `Core-Functionality/` inside the `wp-content/mu-plugins/` directory
2. All is set up for you.